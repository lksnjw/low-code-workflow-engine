import TriggerNode from "./TriggerNode";

/*******************************************************************************
 * Function: EndNode
 *
 * Performs the End Node operation on node for the EndNode module.
 ******************************************************************************/
function EndNode(props) {
  return <TriggerNode label="End" {...props} />;
}

export default EndNode;
